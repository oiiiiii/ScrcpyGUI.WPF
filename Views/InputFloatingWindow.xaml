<Window x:Class="ScrcpyGUI.WPF.Views.InputFloatingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:ScrcpyGUI.WPF.ViewModels"
        mc:Ignorable="d"
        Title="智能输入" 
        Height="180"
        Width="350"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Visibility="Collapsed">
    
    <Window.DataContext>
        <viewModels:InputFloatingViewModel />
    </Window.DataContext>

    <Border CornerRadius="10" 
            Background="#CC1E1E1E" 
            BorderBrush="#33FFFFFF"
            BorderThickness="1"
            Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="智能中文输入"
                           Foreground="White"
                           FontWeight="SemiBold"
                           FontSize="14"
                           VerticalAlignment="Center" />

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <CheckBox x:Name="EnterSendCheckBox"
                              Content="回车发送"
                              IsChecked="{Binding EnableEnterSend}"
                              Foreground="White"
                              FontSize="12"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0" />
                </StackPanel>
            </Grid>

            <TextBox x:Name="InputTextBox"
                     Grid.Row="1"
                     Margin="0,10,0,10"
                     Padding="10"
                     FontSize="14"
                     Background="#333333"
                     Foreground="White"
                     BorderBrush="#555555"
                     BorderThickness="1"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="InputTextBox_KeyDown" />

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="SendButton"
                        Grid.Column="1"
                        Content="发送"
                        Click="SendButton_Click"
                        Background="#007ACC"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,8"
                        FontSize="14"
                        FontWeight="SemiBold" />
            </Grid>

            <TextBlock x:Name="StatusText"
                       Grid.Row="3"
                       Text="{Binding StatusMessage}"
                       Foreground="#FF4CAF50"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0" />
        </Grid>
    </Border>
</Window>
